{% assign gallery_name = "slider" %}
{% assign images = visibleo_gallery.get_by_name.uploads %}


<div id="slider" class="nivoSlider">
	{% for image in images %}
		<img class="slider-img" " width="100%" height="400" src="{{ image.public_url | theme_image_url | resize: '1200x400#'}}">
	{% endfor %}
</div>
